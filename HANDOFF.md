# PitchEditor checkpoint — 2026-09-25 (Asia/Tokyo)

## CURRENT STATE — 2026-09-25 (current local measurements)

### Current development cycle — resynthesis playback race and Git execution boundary (2026-09-25)

- **Next quality defect verified in source:** an asynchronous edit resynthesis previously resumed the playhead after a user stop, seek, background transition or new playback; its `wasPlaying` snapshot was stale after an `await`. `src/app.js` now records the playback generation after its own pause and only auto-resumes if generation, session and visibility are still current, both before and after `rebuildEditedBuffer()`. Seeking while a render has paused playback also invalidates the stale resume intent. Existing solo/background guards and audio DSP are preserved.
- **New `tests/resynth-playback-regression.js`:** isolated actual app-function regression passed for normal auto-resume, intervening stop, seek, hidden state, session replacement, new playback and interruption while rebuilding the audio buffer. This is Node VM control-flow verification, not audible output or browser E2E.
- **Follow-up same cycle:** auto-solo preview after an edit could likewise be queued during an async buffer rebuild, then begin after the page was hidden or the file replaced. A visibility check before rebuilding and a session/visibility/preview-generation check afterward now prevent this stale preview. Expanded the dedicated Node regression with normal preview, hidden preview, preview supersession and session replacement; all PASS. Re-ran `npm.cmd run qa` after this follow-up: PASS. No UI/DSP algorithm changes.
- **Measured QA this cycle:** `npm.cmd run qa` PASS (syntax, engine smoke and existing regression), `node tests/resynth-playback-regression.js` PASS, `node tests/solo-background-regression.js` PASS, `node tests/playback-background-regression.js` PASS, `git diff --check` exit 0 (Windows LF/CRLF warning only). Existing uncommitted source changes remain in place.
- **Current browser attempt:** `py -3.12 tests/solo-browser-e2e.py` failed at Playwright transport's Windows subprocess pipe `CreateFile` with `WinError 5`, before Chromium launched. Historical actual-Chromium E2E passes remain valid for the previously tested version but **this cycle's source changes have not been verified in a real browser**. Do not repeatedly diagnose this known sandbox restriction. Physical iPhone Safari background/foreground, audible output and installed PWA remain **実機確認待ち**.
- **Git environment restriction, not an app defect:** the shared Git administrative directory at `C:\Users\tsuki\Documents\Codex\2026-09-23\pitcheditor-github\.git` denies writes from this Codex execution environment (`index.lock` / `FETCH_HEAD`); hold Git add, commit, push and fetch updates. Keep using read-only local Git; when necessary, the previously proven per-command process-only proxy bypass and OpenSSL TLS route can query the live remote without modifying persistent settings. GitHub auth/connectivity and remote main `c574b4fd9de608a5d7f9acd9ef1ac1066a2ae3e0` were verified earlier; no fresh remote fetch this cycle. Do not retest shared-Git permissions each development cycle.
- **Working tree:** branch `pitch-integration-20260925`, HEAD and stored `origin/main` `c574b4fd9de608a5d7f9acd9ef1ac1066a2ae3e0`. Tracked modification `src/app.js`; untracked `.gitignore`, `AGENTS.md`, `HANDOFF.md`, `tests/playback-background-regression.js`, `tests/reference-race-e2e.py`, `tests/resynth-playback-regression.js`, `tests/solo-background-regression.js`, `tests/solo-browser-e2e.py`; staged none. No commit, push, dependency installation, production publication or Git-admin mutations.
- **Next:** continue non-browser quality work, especially long-track memory and async file replacement; rerun focused real-Chromium E2E including the strengthened reference-race check in an already permitted execution context when available. Keep iPhone Safari audio gesture, OS interruption, Files/share, installed PWA and long-audio memory on the physical-device checklist.

### Latest development cycle — async main/reference playback interruption (2026-09-25)

- **Code inspection revealed a lifecycle race:** ordinary `startPlayback()` and `playReference()` could resume from an asynchronous AudioContext unlock (and main playback from `flushResynth()`) after the page became hidden or an audio session changed. The existing `visibilitychange` listener stopped already active sources but could not cancel those pending starts. This was identified in code; a physical iPhone reproduction has not been established.
- **Minimal local fix in `src/app.js`:** independent monotonic playback generations for main/reference starts; invalidate pending starts whenever their `stopPlayback()` / `stopReference()` runs; check generation, visibility and audio session after awaits; additionally check the current reference request before starting reference playback. Reference import stops prior reference playback when the new request begins. The original solo-background eight-line fix and request-generation guards remain intact. No UI or DSP algorithm changes.
- **New untracked `tests/playback-background-regression.js`:** isolated Node VM lifecycle checks for hidden pending main/reference playback, reference supersession, main cancellation, and successful foreground restarts; **PASS**. `npm.cmd run qa` **PASS** (syntax, engine and existing regression), `node tests/solo-background-regression.js` **PASS**, `node --check tests/playback-background-regression.js` **PASS**, and `git diff --check` found no whitespace errors (LF/CRLF warning only).
- **Browser verification limit in this restricted execution:** attempted `py -3.12 tests/solo-browser-e2e.py`; blocked at Playwright transport subprocess-pipe `CreateFile`, `WinError 5`, before launching the browser. Earlier browser PASS is historical and does not prove the new main/reference changes. The strengthened `tests/reference-race-e2e.py` browser assertion remains unexecuted. No permission escalation, new installation, commit, push, or deployment.
- **Current Git:** integration worktree branch `pitch-integration-20260925`, HEAD and locally stored `origin/main` `c574b4fd9de608a5d7f9acd9ef1ac1066a2ae3e0` (live GitHub unverified). Modified tracked `src/app.js`; untracked `.gitignore`, `AGENTS.md`, `HANDOFF.md`, `tests/reference-race-e2e.py`, `tests/solo-background-regression.js`, `tests/solo-browser-e2e.py`, `tests/playback-background-regression.js`; staged none. Existing Python caches remain ignored and preserved.
- **Next:** run main/reference interruption and strengthened reference-race tests in an already authorized actual-browser environment when available, then inspect and validate the next highest-impact issue, including long-audio memory. Physical iPhone Safari background/resume, audible speaker output, Files/share and standalone-PWA behavior remain unverified.

### Latest development cycle — reference import concurrency (2026-09-25)

- **Follow-up under `approval_policy=never` / `workspace-write`:** strengthened `tests/reference-race-e2e.py` to instrument actual reference playback's `AudioBufferSourceNode.start` and check the post-race buffer remains the 440 Hz new guide. Python AST syntax PASS, `npm.cmd run qa` PASS, isolated solo regression PASS and `git diff --check` has no whitespace errors (only LF/CRLF warning). **The strengthened browser assertion has NOT run:** in-sandbox Playwright transport still fails at Windows `CreateFile` with `WinError 5`; no escalation was attempted under the no-approval setting. Previous browser PASS applies only to the earlier, less stringent version of the test. Next permitted step is to run the strengthened test in an already-authorized browser environment, not to mark it PASS now.

- **Observed defect and cause:** two reference-file loads in one vocal session could finish out of order. The older Worker analysis could overwrite the new guide's pitch track, alignment, suggestions and playback buffer because the original guard checked only the main-audio session ID. Browser regression holds the first guide's Worker response until the second guide has completed and then delivers the obsolete response.
- **Local fix:** `src/app.js` now assigns a monotonically increasing `referenceRequestId` on every reference import and invalidates it on audio-memory release; checks after decode and Worker analysis and in error/finally paths prevent obsolete requests from mutating current reference state. Audio-memory release also invalidates pending solo previews. The earlier eight-line solo-background correction remains in place. No DSP algorithm or UI redesign.
- **Latest measured QA:** `npm.cmd run qa` PASS (syntax, engine smoke, regression); `node tests/solo-background-regression.js` PASS; `tests/reference-race-e2e.py` PASS using real Chromium with 330 Hz old guide held, 440 Hz new guide completed first, and old result delivered late; `tests/solo-browser-e2e.py` PASS; `tests/browser_e2e.py` Chromium default PASS; mobile-mini PASS (previous current cycle); mobile-cycle PASS (latest rerun, repeated audio replacement); `git diff --check` no whitespace errors. Reference-race E2E verifies both actual Worker result pitch values and the application's continued current-reference controls; it does not directly inspect private current reference-track state after the late response.
- **Environment-specific browser access:** Chromium tests pass when permitted to launch outside the restricted Codex sandbox. An in-sandbox mobile-cycle attempt again failed during Playwright's Windows subprocess pipe creation with `WinError 5`, before test code/browser launch. This failure is an execution restriction, not an observed PitchEditor regression. No new browser installation or security configuration change.
- **Git at this checkpoint:** branch `pitch-integration-20260925`, HEAD and locally stored `origin/main` both `c574b4fd9de608a5d7f9acd9ef1ac1066a2ae3e0`. No fresh remote fetch; GitHub's live head remains unconfirmed. Staged: none. Modified tracked: `src/app.js` (earlier solo fix plus request-generation guards). New untracked: `AGENTS.md`, `HANDOFF.md`, `tests/reference-race-e2e.py`, `tests/solo-background-regression.js`, `tests/solo-browser-e2e.py`, `.gitignore`. Existing generated `tests/__pycache__/` is ignored by the new `.gitignore`; bytes preserved, not deleted. No commit or push.
- **Remaining validation:** iPhone Safari physical background/foreground, audible playback, native Files picker/sharing, installed-PWA offline and long-track memory remain unverified. Chromium viewport or synthetic visibility events do not prove physical Safari behavior. Future focus: independently verify current guide contents remain unchanged after a stale response; then inspect long-file memory and iPhone lifecycle risks using existing tooling.

### Latest actual-browser verification — 2026-09-25

- **Browser route established:** Python 3.12 Playwright 1.63.0 and its already installed Chromium launch and run PitchEditor E2E when the existing Codex escalation runs the test outside its restricted sandbox. Inside the current sandbox, Playwright transport creation still fails at Windows async subprocess pipe `CreateFile` with `WinError 5` before browser launch. Ordinary subprocess spawning previously succeeded. No installation or Windows permission/security configuration changes were made. The older browser-blocked findings below are previous measurements and are superseded by this environment-specific result.
- **Measured existing browser QA:** `py -3.12 tests/browser_e2e.py` PASS (Chromium upload → Worker analysis → lazy playback → WAV export); `BROWSER=mobile-mini` PASS (320px mobile/touch, import preflight, note and mobile UI tests, visibility interruption of main playback, WAV export). `BROWSER=webkit` **PARTIAL PASS** for 390×844 mobile shell/viewport and file-picker display; Web Audio constructor unavailable in this Windows WebKit host, so audio/solo was not exercised there.
- **New untracked `tests/solo-browser-e2e.py`:** actual Chromium mobile-sized page loads a WAV, Worker extracts notes, keyboard selects a note, and inspector solo button creates a real `AudioBufferSourceNode`. Browser-dispatched **synthetic** `visibilitychange` with a controlled `document.hidden` stops the old node, leaves no tracked live source, and permits a new foreground solo. A forced pending real `AudioContext.resume()` interrupted by synthetic hidden state does not start stale playback after its Promise resolves; another foreground solo then starts normally. This dedicated test **PASS** using the same escalated Chromium route. It does not demonstrate OS-generated visibility events, audible speaker output, or physical Safari behavior.
- **Measured lightweight QA:** `npm.cmd run qa` PASS (syntax, engine and regression), `node tests/solo-background-regression.js` PASS (isolated listener/async race), `git diff --check` found no whitespace error (only LF/CRLF warning). Existing **8 additions / 0 deletions** in `src/app.js` remain intact and are the only unstaged tracked change. `AGENTS.md`, `HANDOFF.md`, `tests/solo-background-regression.js`, `tests/solo-browser-e2e.py` and generated `tests/__pycache__/` are currently untracked; nothing staged, committed or pushed. Generated Python cache should not be staged.
- **Remaining device verification:** actual iPhone Safari and installed PWA Files picker, gesture-unlock/audio suspension, OS background/foreground, audible solo, orientation/VoiceOver, long-file memory, native sharing and offline relaunch are **unverified**. A Chromium iPhone-sized viewport is emulation, not Safari. Next prioritize true device interruption and audio quality/long-track performance; continue non-device regressions independently. Recheck GitHub remote before any later publication.

- Repository: `C:\Users\tsuki\Documents\Codex\2026-09-23\pitcheditor-integrated`; origin fetch/push: `https://github.com/AmethystNest/vocal-pitch-editor.git`.
- Branch: `pitch-integration-20260925`; HEAD: `c574b4fd9de608a5d7f9acd9ef1ac1066a2ae3e0`; locally stored `origin/main`: same SHA. No fresh remote network check in this task; actual GitHub tip unconfirmed.
- Git: staged changes **none**; unstaged `src/app.js` (**8 added lines**, no deletions); untracked `AGENTS.md`, `HANDOFF.md`. `git diff --stat` reports `src/app.js | 8 ++++++++`; raw diff confirmed solo-preview stop/generation guards. Original `pitcheditor-github` worktree is preserved and was not modified in this task.
- Existing implementation change (uncommitted): on document backgrounding, invalidate pending solo previews, stop and clear the active `S.soloSource`; after asynchronous edited-buffer rebuild and audio unlock, prevent stale or hidden solo playback from starting. Do not overwrite this change or confuse it with proven physical Safari behavior.
- **Current measured QA:** `npm.cmd run qa` PASS (exit 0): JS syntax check, `engine-smoke: PASS`, `regression PASS` (including reference alignment, chunked render and worker analyze). No product source changes were made before this baseline.
- **Current measured browser attempt:** `BROWSER=mobile-mini; python tests/browser_e2e.py` FAILS at `sync_playwright()` transport subprocess pipe creation, `PermissionError [WinError 5]`; browser launch and test code were **not reached**. This is an environment permission barrier, not a demonstrated PitchEditor failure. Separate environment's minimal Chromium launch previously succeeded (historical only); this Codex Native environment has no confirmed working browser path.
- **Current PC mobile verification:** static syntax, engine and regression QA passed; browser viewport, touch, orientation, file input, solo background behavior, audio output, mobile sharing and offline PWA **not executed** due to the Playwright transport failure. Browser E2E modes exist for mobile sizes, long tracks, compressed imports, share outcomes, WebKit and offline PWA, but their existence is not a current pass.
- **Physical iPhone Safari/home-screen PWA:** unverified. Remaining device-only checks: Files WAV/M4A imports, real audio interruption and solo cancellation/recovery, orientation/touch/VoiceOver, memory with long/compressed vocal and reference, native share/Save to Files, offline relaunch and audible output.
- Known unresolved verification gap: no actual browser-level proof yet for the new solo-preview background cancellation; Playwright process creation is blocked in this environment. No new failing product regression established by current lightweight QA.
- Next development task: identify an already available permitted browser path (do not reinstall Playwright solely to bypass this permission issue), add/run a focused solo background/resume regression where possible, protect existing ordinary/reference playback and current source changes, run `npm.cmd run qa` and update this CURRENT STATE with measured results. If browser remains blocked, proceed with isolated non-browser regression/code inspection while marking actual browser and device behavior unverified. Recheck remote before any future publication.
- **Development resumed after baseline:** added untracked `tests/solo-background-regression.js` (isolated Node VM test of the existing `playSegmentSolo` function, with deferred audio unlock). Current measured PASS: backgrounded pending preview cannot start, foreground preview starts, superseded async preview cannot start, latest preview starts. Re-ran `npm.cmd run qa`: PASS; `git diff --check`: no whitespace errors (only Windows LF/CRLF warning). This test is not browser E2E and does not prove the real `visibilitychange` event wiring, audio output or Safari behavior. Updated Git untracked list: `AGENTS.md`, `HANDOFF.md`, `tests/solo-background-regression.js`; `src/app.js` remains the only unstaged tracked change; staged none. Next: browser integration coverage via permitted path and physical iPhone device matrix when available.

## HISTORY — previous checkpoints (historical, not current state)

Read `AGENTS.md` first. This snapshot was assembled from the accessible local checkout, its Git history, current working-tree changes, `README.md`, `docs/OSS-RESEARCH.md` and tests. The earlier Work conversation was partially compacted and its full text was unavailable here; facts not supported by those sources are marked **unconfirmed**. The initial handoff task created documentation only; subsequent Codex CLI resumption details are recorded below.

## Product AGENTS organization — 2026-09-25

- PitchEditor AGENTS has been reorganized into product scope, implementation/privacy constraints, DSP quality, iPhone/Safari UX and product QA. Common autonomy, tool selection, RTK, context efficiency, Git safety and generic HANDOFF rules are inherited from the shared master. Removed stray leading plus signs from the saved status-management section.
- This documentation-only task preserves the original dirty worktree, the integration worktree app.js change and both untracked docs. No product code editing, fresh product QA, commit, push, deploy or installation was performed. Earlier npm run qa PASS pertains to the preexisting solo preview code change. Browser background/resume and physical iPhone Safari remain unverified.
- Future HANDOFF cleanup: keep a single current checkpoint at the top and retain historical integration details below without repeating stale branch/status as current. The next development task after this documentation stop remains browser-level solo-preview background/resume regression.

## Autonomous development checkpoint — 2026-09-25

- Added the permanent autonomous development cycle, approval boundaries, verification distinctions, alternative-testing requirement, Git preservation and ongoing HANDOFF maintenance to this integration worktree's `AGENTS.md`. These documents remain untracked and have **not** been committed or pushed.
- Continued the upstream-based integration at `pitch-integration-20260925`, HEAD and stored `origin/main` `c574b4f`. The original `pitcheditor-github` worktree at `da40d48` remains untouched with its dirty working tree. The integration worktree has modified `src/app.js` and untracked `AGENTS.md` / `HANDOFF.md`; no commits, pushes or deployments were made. A fresh remote fetch was completed during the earlier integration, but no additional fetch was run at this checkpoint.
- In `src/app.js`, hiding the document now invalidates pending solo-note previews and stops/clears the active `S.soloSource`. `playSegmentSolo` also checks the generation and document visibility after asynchronous buffer rebuild and audio unlock, preventing a stale async continuation from starting audio after backgrounding. This is a **code change**, not a claim of confirmed physical-Safari resolution.
- After the latest generation-guard edit, `npm run qa` **passed**: syntax checks, `engine-smoke` and regression tests. `git diff --check` reported no whitespace error (only a Windows LF-to-CRLF warning). These tests do not exercise the actual backgrounded solo-preview browser behavior.
- Python Playwright browser E2E previously failed at process creation with Windows `WinError 5`. A search for a browser/CDP tool in the current Codex tool registry returned no matching tool; this checkpoint did not establish an alternative working browser path. Physical iPhone Safari / home-screen PWA and the actual solo-preview interruption remain **unverified**.
- **Next safe task:** add/run a targeted browser-level solo-preview background/resume test using an accessible browser route; verify in-flight preview cancellation and regression for ordinary playback. Then perform the README physical iPhone/Safari/PWA matrix when a device is accessible. Check remote divergence again before any later integration or publication. Do not discard the original worktree or silently overwrite its changes.

## Safe GitHub integration — 2026-09-25 (current checkpoint)

- **Active integration worktree:** `C:\Users\tsuki\Documents\Codex\2026-09-23\pitcheditor-integrated`, branch `pitch-integration-20260925`, based directly on fetched `origin/main` `c574b4f` (58 commits ahead of original `da40d48`). The original `pitcheditor-github` worktree and its tracked/untracked changes are preserved without reset, stash, checkout, commit or push. Worktree branch has no new commits.
- Inspected all 58 commit subjects and compared changed paths and the local-vs-remote working diff. GitHub's v50–v53 additions provide visible compact-toolbar scroll chevrons, narrow-phone 44px fine-adjustment controls, inspector stacking on rotation, updated mobile E2E and published GitHub Actions. Existing local v49 work is already included upstream, apart from small wording differences. Replacing the newer remote UI/tests/cache with older local versions would regress these fixes; therefore the integrated worktree retains upstream versions.
- Transferred the two untracked handoff documents and the local background `S.soloSource` stop/clear fix into the integration worktree. Original investigation logs, Playwright captures and Python cache remain intact in the original worktree; they are not app source and were not imported. The old worktree is a complete reference for every local modification.
- Historical statements below describing locally stored `origin/main` as `da40d48`, QA workflow as untracked and v49 as the latest are snapshots of the original worktree, **not** the current integration state. The active worktree has the upstream v53 UI, v53 service-worker cache and tracked CI workflow. Do not use the old snapshot to revert current upstream behavior.
- This integration intentionally stops before feature development, commit, push or deployment. Physical iPhone/Safari and Playwright browser E2E still require separate verification; `npm run qa` outcome for this integrated worktree is recorded in the current task report.

## Codex CLI resumption — 2026-09-25

- Confirmed this repository by the paired `AGENTS.md` and `HANDOFF.md`, package, source and Git remote. Local `main` remains at `da40d48` with existing uncommitted changes preserved.
- Fresh `git fetch origin main` succeeded: fetched `origin/main` is `c574b4f`, 58 commits ahead of local `main`. Remote has newer mobile toolbar and pitch inspector fixes and CI files. The working tree differs from this latest remote; protect and reconcile changes before merging or pulling.
- Reproduced the code-level gap at the Work interruption: the hidden `visibilitychange` handler stopped main/reference playback but did not stop the independent solo-note `S.soloSource`. Added a minimal stop/clear there in `src/app.js`. This edit is local, uncommitted and not deployed. An in-flight asynchronous solo-preview may additionally require a stale-start guard if it reproduces.
- `npm run qa` passed (syntax, engine and regression); `git diff --check` found no whitespace errors. Browser E2E could not start Playwright because Windows denied Python subprocess pipe creation (`WinError 5`). Physical iPhone Safari / installed PWA are unverified.
- Resume by safeguarding the working tree, reconciling with the 58 newer remote commits, testing the solo-preview background interruption via browser E2E where permitted, and doing physical Safari checks. No commit, rebase, merge, push or deployment was performed in this resumption.

## Latest verified Git boundary

- **Confirmed locally:** branch `main`, configured upstream `origin/main`, remote `https://github.com/AmethystNest/vocal-pitch-editor.git`. Local `HEAD` and locally stored `origin/main` both point to `da40d48` (2026-09-24 06:40 JST, `Cover standalone AAC on mobile browsers`). This proves equality with the locally stored remote-tracking ref, **not** that a fresh network check or GitHub Pages publication succeeded.
- **Confirmed local history:** preceding commits `3184cb3` (mobile audio interruption recovery), `1b99223` (landscape inspector), `fcfa86a` (larger touch targets), `58d9b04` (accessible first run), `82a37e7` (mobile WAV export), `efa79cf` (PWA revalidation), `fe989d1` (page zoom/PWA launch assets).
- **Confirmed uncommitted tracked edits at this checkpoint:** `README.md`, `index.html`, `src/app.js`, `sw.js`, `tests/browser_e2e.py`. The current source/README describes v40–v49 changes not present in the latest local commit, including reference no-op feedback, VoiceOver boundary navigation, reduced motion, interrupted horizontal pan recovery, and memory preflight for MP3/M4A/AAC and no-Xing VBR. `sw.js` currently names `vocal-pitch-editor-v49-mp3-vbr-preflight`.
- **Confirmed untracked local material:** `.github/` (includes `workflows/qa.yml`), `.playwright-mcp/`, `audio-life.txt`, `export-flow.txt`, `share-outcomes.txt`, `share-test-later.txt`, `share-tests.txt`, `tests/__pycache__/`, `tests/requirements-browser.txt`, `tick-life.txt`. Some are generated investigation logs/cache; inspect individually before staging. This handoff also introduces `AGENTS.md` and `HANDOFF.md` as new local files. Do not automatically commit private/debug logs or Python cache.
- **Unconfirmed:** whether GitHub has newer commits than the stored `origin/main`, whether any local changes were published from a different checkout, the live GitHub Actions result, the currently deployed Pages version and real iPhone Safari behavior. Fetch/compare before any later push, with explicit care for uncommitted work.

## Current implementation and quality state

- **Confirmed in source:** `index.html` plus `src/app.js`, `src/engine.js` and `src/worker.js` implement the Canvas note editor, Web Audio playback/file decode, YIN F0/note extraction in Worker, reference/DTW matching, TD-PSOLA correction and WAV export. `sw.js` precaches the app shell for PWA operation. The package is `vocal-pitch-editor` version `22.0.0`; the README's v49 headings and SW cache version are separate development markers, not the npm package version.
- **Confirmed design decisions:** `docs/OSS-RESEARCH.md` (2026-09-24) selected Autotone as the closest product architecture reference, OpenVox as a secondary mobile/PWA comparison, and openDAW as a DAW-scope boundary. It retained the current lightweight local-processing stack, without wholesale source reuse.
- **Documented earlier successful checks:** README reports Windows Playwright Chromium upload → Worker analysis → 24-bit WAV export PASS, alongside numerous mobile mode regressions. These are historical results; **not re-run or independently confirmed for this checkpoint's uncommitted v49 tree**.
- **Present test infrastructure, not proof of current pass:** `npm run check`, `npm run test:engine`, `npm run test:regression`, combined `npm run qa`, and `npm run test:browser`. Local `.github/workflows/qa.yml` specifies Node 22, Python 3.12, Playwright Chromium/WebKit and FFmpeg, with mobile-mini, mobile-long, mobile-cycle, webkit, MP3/M4A/AAC, mobile-share-success and offline PWA runs. Workflow is untracked, so GitHub CI adoption is **unconfirmed**.
- **Unfinished / unverified:** real iPhone Safari plus installed home-screen PWA end-to-end checks; real share sheet and Save to Files; real iPhone audio interruption, landscape/touch interruption, VoiceOver, long/compressed file memory behavior and audible correction quality. Do not label these verified on the strength of emulation. Further product backlog and final numerical targets are **unconfirmed**.

## Most recent work, interruption point and known risks

- **Confirmed by the working diff:** after `da40d48`, the local Work session implemented and documented incremental mobile accessibility and iPhone memory checks through **v49**, and expanded `tests/browser_e2e.py`. A local untracked QA workflow and test dependency file also exist. Several untracked `*-life.txt`, `share-*.txt` and `export-flow.txt` files appear to capture investigation output; their completeness and result status have not been independently authenticated.
- **Confirmed interruption point for this handoff:** development was paused with five tracked files modified and multiple untracked files. No new feature work, staging, commit, push or deployment is part of the current documentation task.
- **Known problem classes addressed by recent code, with regression risk:** iPhone decode/peak-memory exhaustion (including compressed audio and combined vocal/reference), gesture cancellation on rotation/background, suspended AudioContext on resume, compact/landscape inspector reachability, VoiceOver boundary focus, native-share gesture timing, and stale offline shell. **Unconfirmed:** whether any still reproduces on physical Safari. Other active reproducible bugs have not been established from the inspected evidence.
- **Next resumption:** (1) read `AGENTS.md`/this checkpoint and verify status/diff without discarding changes; (2) inspect the v40–v49 local diff and investigation logs to recover any unrecorded failed/passed cases; (3) run `npm run qa` and the affected compact iPhone, import-memory, share and offline E2E modes; (4) test real iPhone Safari/PWA where accessible; (5) only after review, stage appropriate source/tests/docs/workflow, commit and compare/fetch remote before an authorized push. Request for this turn explicitly stops **before** resuming development or publishing.

## How to run and verify

From the repository root in PowerShell (prefix commands with `rtk` where configured by the user's CLI-level `RTK.md`):

```powershell
npm run check
npm run qa
npm run serve
python -m pip install -r tests/requirements-browser.txt
python -m playwright install chromium webkit
$env:BROWSER='mobile-mini'; python tests/browser_e2e.py
$env:BROWSER='mobile-share-success'; python tests/browser_e2e.py
$env:BROWSER='pwa-offline'; python tests/browser_e2e.py
```

`npm run serve` uses `npx serve .`; Worker and PWA paths need HTTP(S), and service-worker installation requires HTTPS (or an appropriate localhost environment). Browser tests use Python Playwright; FFmpeg is needed for compressed-input fixtures; `cryptography` for the temporary HTTPS PWA test. Other `BROWSER` modes and device-validation procedures are listed in `README.md` and `tests/browser_e2e.py`. Check browser console errors, corrected-note/Undo behavior, reference guidance, WAV sample/container characteristics, interruption/recovery and the installed/offline shell. Record the exact commands, environment and outcomes on resumption; none of these commands is claimed to have run for this handoff.
